This folder is populated by the `query-codegen` script in `package.json`. It
is needed because apollo-codegen, doesn't understand that a path should be
used as a folder unless a folder exists at the path.